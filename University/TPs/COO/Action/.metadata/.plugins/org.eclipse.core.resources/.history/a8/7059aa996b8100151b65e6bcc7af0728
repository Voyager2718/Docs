package action;

import org.junit.Test;

import resources.Basket;

public abstract class BasketPoolTest extends ResourcePoolTest<Basket> {
	@Test
	public void getDescription() {
		Basket b = this.resourcePool.provideResource();
		assert(b.description().equals("This is a basket."));
	}
}